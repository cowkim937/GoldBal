{
  "name": "hwangbal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "vite build && wrangler pages deploy dist",
    "release": "vite build --mode production && wrangler pages deploy dist --project-name hwangbal"
  },
  "devDependencies": {
    "vite": "^8.0.12",
    "wrangler": "^4.103.0"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "bootstrap": "5.3.3",
    "firebase": "^12.15.0"
  }
}
