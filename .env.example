# .env는 Vite 빌드타임 변수용입니다 (현재 미사용).
# 모든 Firebase 설정은 런타임에 Worker /api/firebase-config 에서 제공됩니다.
# → Cloudflare 시크릿은 scripts/sync-secrets.sh 로 ../apis.txt에서 일괄 등록하세요.
# → 로컬 개발 시 .dev.vars.example을 .dev.vars로 복사 후 값 채우고 wrangler dev 실행.
